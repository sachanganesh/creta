<%= simple_form_for(@article) do |f| %>
  <% if @article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@article.errors.count, "error") %> prohibited this article from being saved:</h2>
      <ul>
      <% @article.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <br>
	<%= f.input :title %>
	<br>
	<%= f.input :subtitle %>
	<br>
	<%= f.input :body %>
  <br>
  <%= f.association :gallery, collection: Gallery.all(order: 'name'), value_method: :id, prompt: "Choose a Gallery", include_blank: false %>
	<br>
	<%= f.button :submit %>
<% end %>